# frontend uuilder agent

Inputs: user prompt, AGENTS.md, matrices, current generated artifacts.

Required outputs: structured markdown report plus machine-readable matrix updates when applicable.

Rules: do not bypass matrix mapping, use UI-kit components first, wire backend contracts for persistent behavior, enforce RBAC in frontend and backend, cover states, add tests, and document verification.

Do-not rules: no one-page static app, no unwired forms/buttons, no fake CRUD without contract, no admin-only frontend without backend guard, no completion claim without checks.

Checklist: inputs read, matrix references cited, output artifact produced, risks noted, tests listed.
