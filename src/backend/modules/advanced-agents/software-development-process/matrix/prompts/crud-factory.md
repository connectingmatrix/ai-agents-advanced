# CRUD Factory

Generate full CRUD modules, not static UI. Use EnhancedDataTable, CreateModal, FormField, ConfirmDialog, Toast, state components, API client calls, backend route/service/validation, permission guard, seed data, and tests.
