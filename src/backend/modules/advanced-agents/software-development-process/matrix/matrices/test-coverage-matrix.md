# Test Coverage Matrix

Every feature needs route, component, backend, integration, RBAC, validation, error-state, and build coverage. CRUD tests must prove create/edit/delete reach the backend contract.
