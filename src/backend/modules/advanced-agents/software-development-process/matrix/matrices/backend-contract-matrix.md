# Backend Contract Matrix

Every frontend action must map to a backend contract. Components may not invent URLs. Contracts must define method, auth, roles, permissions, request, response, errors, validation, persistence operation, and tests.
