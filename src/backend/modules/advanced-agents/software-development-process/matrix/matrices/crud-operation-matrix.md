# CRUD Operation Matrix

CRUD requires list/create/read/update/delete contracts, UI actions, validation, permission guards, state coverage, and tests. Static tables are forbidden when CRUD is required.
